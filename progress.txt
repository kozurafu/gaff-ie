## Codebase Patterns
- Prices stored in EUROS (not cents) — never divide by 100
- Use `getTokenFromRequest(request)` for API route auth, `getCurrentUser()` for server components (uses cookies)
- JWT secret: 'gaff-production-secret-x7k9m2p4' (prod), cookie name: 'gaff-token'
- Tailwind v4: use `@import "tailwindcss"` and `@theme` block in globals.css, NOT tailwind.config.js
- Color: gaff-teal is #0C9B8A, gaff-slate is #1E293B, gaff-warm is #FAFAF9
- All new models need `npx prisma db push` to sync with the external DB at 62.171.142.50:6000
- AgentMail API: POST to https://api.agentmail.to/v0/inboxes/mmclaw@agentmail.to/messages/send with Bearer token
- Listing creation now runs scam detection — scores >70 auto-flag to DRAFT
- The app runs on HTTP (not HTTPS) — cookies must NOT have secure:true
- `output: 'standalone'` in next.config.ts is required for Docker/Coolify deployment
- FurnishedStatus enum: YES | NO | OPTIONAL
- ListingType enum: RENT | SALE | SHARE
- PropertyType enum: APARTMENT | HOUSE | STUDIO | ROOM_SHARE | DUPLEX | DETACHED | SEMI_DETACHED | TERRACED | BUNGALOW | PENTHOUSE
- UserRole enum: TENANT | LANDLORD | AGENT | ADMIN

---
## 2026-03-25 — Initial Build (Pre-Ralph)
- Full-stack app built: 50+ source files, 29+ routes
- Auth system (JWT cookies, register/login/logout)
- Listing CRUD with search, filters (location, price, beds, type, HAP, pets, parking, furnished, BER)
- Messaging system (WhatsApp-style inbox, enquiry flow, unread badges)
- Tenant preference profiles ("My Ideal Gaff")
- Verification system (email auto-verify, Stripe Identity for ID, manual review for property/employment)
- Saved listings and saved searches
- Listing edit page + status management (let agreed, withdraw, reactivate)
- Image upload API
- Scam detection integrated into listing creation
- Admin queues (verifications, flagged listings)
- About, Contact (AgentMail), FAQ pages
- Report listing modal
- Homepage redesigned: search-first, no fake metrics
---

## 2026-03-25 — T-001: Error handling and loading states (Ralph)
- Created src/app/not-found.tsx — custom 404 page with link home
- Created src/app/error.tsx — custom 500 page with retry button
- Created src/components/ui/Toast.tsx — ToastProvider + useToast hook, success/error/info variants, auto-dismiss 5s
- Updated src/app/layout.tsx — wrapped app in ToastProvider
- Build passes ✅
---

## 2026-03-25 — T-002: Mobile-responsive navbar with auth state (Ralph)
- Rewrote src/components/ui/Navbar.tsx with full mobile optimization
- Added user name + role display in mobile slide-in header (avatar circle + name + role badge)
- Role-aware dashboard label: "My Properties" (landlord), "Portfolio" (agent), "My Dashboard" (tenant)
- Smooth open/close animation with closing state (slide-out before unmount)
- Body scroll lock when mobile menu is open
- Messages icon with unread badge shown in mobile top bar (next to hamburger)
- Icons added to all mobile menu items for better visual hierarchy
- Close button at top of slide-in panel
- Desktop dropdown also uses role-aware dashboard label
- Build passes ✅
---

## 2026-03-25 — T-003: User profile page with edit mode (Ralph)
- Created src/app/api/users/[id]/profile/route.ts — GET returns full user profile (name, role, avatar, verifications, listings, reviews, tenant preferences), PUT updates name/phone/bio/avatar with auth check
- Created src/app/user/[id]/page.tsx — public profile page with role-appropriate content: trust score + breakdown, verification badges, bio, company info (landlord), tenant preferences summary, active listings grid
- Created src/app/dashboard/profile/page.tsx — edit form for name, phone, bio, avatar URL with toast notifications, links to public profile
- All roles supported: tenants show preferences summary, landlords/agents show active listings
- "Edit profile" link shown on own profile page
- Build passes ✅
---

## 2026-03-25 — T-004: Search sort and URL state persistence (Ralph)
- Updated src/app/api/listings/route.ts: API now accepts ?sort=newest|price_asc|price_desc, orders results accordingly
- Rewrote src/app/search/page.tsx: all filter state syncs to URL params via router.replace()
- All filters (location, minPrice, maxPrice, bedrooms, propertyType, listingType, hapAccepted, petsAllowed, parking, furnished, berRating, sort, page) read from URL on load
- Sort values changed from 'price-asc'/'price-desc' to 'price_asc'/'price_desc' to match API convention
- Browser back/forward restores previous search state (using replace, not push, to avoid excessive history entries)
- More filters section auto-expands if any advanced filter is active from URL
- Build passes ✅
---

## 2026-03-25 — T-005: Listing status lifecycle and freshness (Ralph)
- Updated src/components/ui/ListingCard.tsx: added `createdAt` prop, `getFreshnessBadge()` helper — shows "✨ New" badge for <3 days, amber "Listed X weeks ago" for >14 days
- Updated src/app/api/listings/route.ts: auto-expires ACTIVE listings older than 30 days on every GET (bulk updateMany)
- Updated src/app/api/listings/[id]/route.ts: PATCH renew support — reactivating an EXPIRED listing resets createdAt to now
- Updated src/app/dashboard/page.tsx: added 🔄 Renew button for EXPIRED listings
- Updated src/app/search/page.tsx and src/app/page.tsx: pass createdAt to ListingCard
- Build passes ✅

## 2026-03-25 — T-006: Smart matching integration with tenant preferences (Ralph)
- Rewrote src/lib/matching.ts to use TenantPreference model instead of Profile model
- Match scoring: budget (0-35), location (0-25), bedrooms (0-10), amenities (0-20), moveInDate (0-10) = 100 max
- Amenities scoring considers: HAP, pets, parking, furnished, propertyType, listingType from amenityPrefs JSON
- Added getMatchScoreForListing() helper for single-listing lookups
- Updated src/app/api/listings/recommended/route.ts: uses getTokenFromRequest (API route pattern), returns matchScore + breakdown
- Created src/app/api/listings/[id]/match/route.ts — GET returns match score for a specific listing for the logged-in tenant
- Updated src/app/listing/[id]/page.tsx: shows "🎯 X% match" badge next to listing type (green ≥70%, amber ≥40%, gray <40%)
- Updated src/app/dashboard/page.tsx: tenant dashboard "AI Matches" section shows top 5 recommended listings with scores, images, and links
- AI Matches stat card now shows actual count instead of "Coming soon"
- Build passes ✅

## 2026-03-25 — T-008: Viewing scheduler (Ralph)
- Added ViewingSlot and ViewingBooking models to Prisma schema with proper relations
- Created API: GET/POST /api/listings/[id]/viewings (list/create slots — owner only can create)
- Created API: POST/DELETE /api/viewings/[id]/book (book/cancel — tenants only)
- Created API: GET /api/viewings/my (returns upcoming viewings for current user, role-aware)
- Updated listing detail page: shows "Upcoming Viewings" section with book button, inline booking flow with optional message
- Updated dashboard: added UpcomingViewings component for all roles (tenant sees booked viewings, landlord/agent sees slots with attendee lists)
- Slots show spots remaining, full indicator, and booked confirmation
- Auth uses `user.sub` from JWTPayload (not userId) — important pattern for getTokenFromRequest
- Build passes ✅
---

## 2026-03-25 — T-007: Read receipts and response time tracking (Ralph)
- Schema already had `readAt` on Message model — no migration needed
- Conversation GET already marks messages as read when opened — no change needed
- Updated src/app/messages/page.tsx: sent messages now show ✓ (sent) or ✓✓ (read) indicators with hover tooltip showing read time
- Created src/app/api/users/[id]/response-time/route.ts: calculates average response time from message history, returns human-readable label ("Usually responds within X hours")
- Updated src/app/user/[id]/page.tsx: landlord/agent profiles show response time with clock icon in gaff-teal
- Response time calculation: finds pairs of incoming→reply messages per conversation, averages the deltas, ignores gaps >7 days
- Build passes ✅

## 2026-03-25 — T-009: Full-text search (Ralph)
- Updated src/app/api/listings/route.ts: accepts `?q=keyword` parameter, searches across title, description, addressLine1, city, county using Prisma `contains` with case-insensitive mode (OR across all fields)
- Updated src/app/search/page.tsx: added keyword search input with magnifying glass icon in the filter bar, syncs to URL params as `q=`, included in fetchListings API call
- Query state persists in URL (shareable/bookmarkable) and restores on page load
- Used `contains`+`insensitive` instead of raw SQL tsvector for simplicity and Prisma compatibility — works well for keyword matching
- Build passes ✅

## 2026-03-25 — T-011: Agent organization profiles and team management (Ralph)
- Added description, website, phone fields to AgentOrg model in Prisma schema
- Created API: POST /api/agencies (create agency — agent only, auto-adds as OWNER)
- Created API: GET /api/agencies/[id] (public profile — includes members, listings, avg rating)
- Created API: PUT /api/agencies/[id] (update — owner only)
- Created API: GET /api/agencies/my (get current user's agency)
- Created /agency/[id] page: public agency profile with header, description, contact, team grid, active listings grid
- Updated agent dashboard: AgencySection component shows agency info + team avatars if member, or "Create Your Agency" CTA with inline form
- Agency creation form: name, description, website, phone
- Build passes ✅
---

## 2026-03-25 — T-010: Email notifications for new matching listings (Ralph)
- Created src/lib/listingNotifications.ts: self-contained module with match scoring (duplicated from matching.ts to avoid server component import issues) and AgentMail email sending
- notifyMatchingTenants() is fire-and-forget — called after listing creation, doesn't block response
- Finds all tenants with alertFrequency='instant' and emailAlerts=true, calculates match score, sends email if >= 70%
- Rate limited to max 10 emails per new listing to prevent spam
- Only notifies for ACTIVE listings (skips auto-flagged/DRAFT from scam detection)
- Email includes: title, price, city, bedrooms, match %, link to listing, HTML + plain text versions
- Updated src/app/api/listings/route.ts: imports and calls notifyMatchingTenants after scam detection
- Build passes ✅

## 2026-03-25 — T-013: SEO and structured data (Ralph)
- Created src/app/robots.ts — allows all crawlers, blocks /api/, /dashboard/, /admin/, /auth/
- Created src/app/sitemap.ts — dynamic sitemap with static pages + all ACTIVE listings from DB
- Added JSON-LD structured data (RealEstateListing schema) to listing detail page via script tag
- Dynamic document.title on listing detail page ("€X - Title | Gaff.ie")
- Layout.tsx already had og:title, og:description, og:image — confirmed
- Created layout.tsx with metadata for: search, faq, contact, auth/login, auth/register
- About page already had metadata export
- Build passes ✅
---

## 2026-03-25 — T-012: Admin dashboard with analytics (Ralph)
- Created src/app/api/admin/stats/route.ts: aggregates all platform stats (users by role, listings by status, messages, verifications by status, recent reports/flagged/registrations)
- Created src/app/admin/page.tsx: full admin dashboard with 4 stat cards (users, listings, messages, verifications) and 3-column activity feed (recent reports, flagged listings, recent registrations)
- Admin-only access enforced via role check in API
- Links to existing /admin/verifications and /admin/flagged pages
- Time-ago formatting for activity feed items
- Loading skeleton with animated pulse
- Build passes ✅
